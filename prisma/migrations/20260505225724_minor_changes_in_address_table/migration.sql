BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[address] ADD CONSTRAINT [address_isActive_df] DEFAULT 1 FOR [isActive];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
